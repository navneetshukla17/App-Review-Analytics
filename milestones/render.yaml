services:
  - type: web
    name: review-insights
    env: docker
    plan: free
    healthCheckPath: /api/apps
    dockerContext: milestones
    dockerfilePath: milestones/Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false # User needs to enter the Neon PostgreSQL URL here
